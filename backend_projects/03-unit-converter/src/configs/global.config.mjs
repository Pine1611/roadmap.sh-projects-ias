const { SERVER_PORT } = process.env;
const DECIMAL_DIGITS = 6;
const SYSTEMS_OF_MEASUREMENT = {
    LENGTH: "length",
    TEMPERATURE: "temperature",
    // WEIGHT: "weight",
};
export { SERVER_PORT, DECIMAL_DIGITS, SYSTEMS_OF_MEASUREMENT };
